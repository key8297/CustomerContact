@using WebHelpers.Mvc5
<aside class="main-sidebar">
    <section class="sidebar">
        @* BEGIN Search *@
        @*<form action="#" method="get" class="sidebar-form">
            <div class="input-group">
                <input type="text" name="q" class="form-control" placeholder="Search...">
                <span class="input-group-btn">
                    <button type="submit" name="search" id="search-btn" class="btn btn-flat">
                        <i class="fa fa-search"></i>
                    </button>
                </span>
            </div>
        </form>*@
        @* END Search *@
        @* BEGIN Menu *@
        @{
            string url = Request.Url.ToString().ToLower();
            string isCustomer = (url.Contains("/customer/") || url.EndsWith("/customer")) ? "active" : "";
            string isContact = (url.Contains("/contact/") || url.EndsWith("/contact")) ? "active" : "";
        }
        <ul class="sidebar-menu" data-widget="tree">
            <li class="header">MENU</li>
            <li class="@isCustomer">
                <a href="@Url.Action("Index", "Customer")"><i class="fa fa-link"></i> <span>Customer</span></a>
            </li>
            @*<li class="@isContact">
                <a href="@Url.Action("Index", "Contact")"><i class="fa fa-link"></i> <span>Contact</span></a>
            </li>
            <li class="treeview">
                <a href="#">
                    <i class="fa fa-link"></i> <span>Multilevel</span>
                    <span class="pull-right-container">
                        <i class="fa fa-angle-left pull-right"></i>
                    </span>
                </a>
                <ul class="treeview-menu">
                    <li><a href="#">Link in level 2</a></li>
                    <li><a href="#">Link in level 2</a></li>
                </ul>
            </li>*@
        </ul>
        @* END Menu *@
    </section>
</aside>